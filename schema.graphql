type Paper @entity {
  id: ID!
  owner: Bytes! # address
  author: String!
  tokenUri: String!
  allowFunding: String!
  fundAmount: String!
  totalAmountFunded: String!
}

type Funding @entity {
  id: ID!
  from: Bytes! # addres in bytes
  to: Bytes!
  amount: String!
  paperid: String!
}
